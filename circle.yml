---
machine:
    python:
        version: 3.4.2

dependencies:
    override:
        - pip install coveralls

test:
  override:
    - ./.circleci/run_tox.sh 2.6.8 2.7.9 3.2.5 3.3.3 3.4.2
    - coveralls
